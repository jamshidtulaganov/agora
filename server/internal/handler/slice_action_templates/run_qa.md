Run QA for this issue as a DETERMINISTIC gate — report strictly by EXIT CODE, never by opinion, and do NOT weaken, skip, or delete any test to make it pass. Judge the CHANGE, not the repo: a check that is ALREADY red on the base branch is PRE-EXISTING and must NOT fail this gate — only a NEW failure this change introduces does. (1) CHECKS — THE BRANCH FIRST: on the change's branch, detect the project type and run its build + lint + tests, recording each command and its exit code — e.g. `pnpm build && pnpm lint && pnpm test` (JS/TS), `go build ./... && go test ./...` (Go), `php -l` on changed files plus phpunit/codeception (PHP). AGORA-QA MCP: when the `agora-qa` MCP server is available, PREFER its tools over hand-composed shell for the test half of this — `detect_tests` resolves the repo's runner + command, `run_tests` runs the suite and returns the REAL `{exit_code, passed, output}` (deterministic, nothing to parse), and `run_case_script` runs one self-contained case script (set `trace_path` to capture the Playwright trace). Build/lint still run as normal shell commands. (2) BASELINE — ONLY FOR WHAT WENT RED, NEVER UP FRONT: if EVERY command in step 1 passed, there is NO baseline step — do NOT check out the merge-base, do not re-run anything, go straight to the smoke. A command that is GREEN on the branch cannot be a new failure whatever the base does, so running it against the base decides nothing; on the common all-green branch that is HALF THE GATE spent on a foregone conclusion. ONLY when at least one command went red: check out the merge-base (the base branch this PR/MR targets, e.g. `main`) and re-run JUST the commands that failed, recording each exit code. Then classify each red command: red on BOTH is PRE-EXISTING (note it, do not block); green on the base but RED on the branch is a NEW failure this change caused — that fails the gate. Every command that passed on the branch is simply `pass`, with no baseline result to report. (3) SMOKE — DETERMINISTIC-FIRST, vision-last: decide the smoke verdict from DETERMINISTIC signals (HTTP status, console output, network responses, and asserted DOM / accessibility-tree TEXT), NEVER by visually judging a screenshot. If the project configures a smoke command (see its QA smoke below), RUN IT and take its EXIT CODE as the smoke verdict — a deterministic login-and-assert script (curl/CLI that checks status + page text and exits 0/1) is faster, cheaper, and more reliable than driving the UI by hand and consumes no vision tokens. Otherwise bring the app up and exercise it in a real browser — prefer the co-code editor's embedded Chromium over CDP (get the preview URL and the Chromium CDP url from the local daemon's editor endpoints, then drive it with `playwright-core` `chromium.connectOverCDP(<cdp_url>)`); if you cannot reach the embedded browser, launch your own headless Chromium. Read the page via its DOM / accessibility-tree snapshot (text), not a screenshot, and assert ALL of: (a) NO console errors AND no console warnings — in particular a vue-i18n / intlify "Not found '<key>' key" or any missing-translation warning is a FAIL; (b) no 4xx/5xx network responses; (c) the main UI renders — assert specific expected elements/text are PRESENT in the DOM or accessibility tree (assert on TEXT, not pixels); and (d) NO untranslated placeholder keys are visible in the rendered text — a raw i18n key showing through (a dotted identifier such as `section.tile.title` displayed verbatim) means a translation was never registered and is a FAIL, even when nothing logged. Apply the same lazy-baseline rule to smoke findings: a CLEAN smoke needs no base comparison at all — only when the smoke surfaces a console error, network failure, or placeholder do you load the unchanged base page to classify it. One that ALSO reproduces there is pre-existing; one that appears only after the change is a NEW failure. Capture a screenshot ONLY to DOCUMENT a failure you have already determined from the assertions above (attach it to the verdict for the human) — do NOT screenshot the happy path, and NEVER vision-analyze a screenshot to decide pass/fail; every smoke verdict must trace to a deterministic signal. EVIDENCE FLOOR for a UI change: whenever you drive a real browser to assert a UI case, CAPTURE A PLAYWRIGHT TRACE (`context.tracing.start(...)` → `stop({path: TRACE_PATH})`) and record that trace path on the run — a trace is a DETERMINISTIC artifact (a DOM/step recording, NOT a visual judgment), it is cheaper than a screenshot, and it is what proves a guarded or large-diff UI case was actually driven. A UI-case `pass` on a high-blast-radius change (risk:guarded / risk:critical, or a large diff) that carries NEITHER a trace NOR a screenshot is rejected as under-evidenced and downgraded to `qa:stale` — so capture the trace as a matter of course when you open a browser. (A trivial / tiny-diff / no-PR change does NOT need a trace — its deterministic command result is sufficient — so don't add the ceremony there.) (4) TEST CASES — PREFER THE PRE-AUTHORED SUITE, don't re-author it. TDD FAST-PATH: the developer works test-first and COMMITS the tests with the code, so the branch usually already carries tests encoding this issue's criteria. When it does, THE COMMITTED TESTS ARE THE SUITE: run them via the repo's own test command (`run_tests`), verify they actually assert the plan's criteria (a test green on the merge-base locks nothing — flag it), and skip authoring anything they already cover. Your added value is then only the baseline comparison, the smoke, and any UNCOVERED criterion — which makes this gate a command run, not an authoring session. A QA agent authors this issue's test cases FROM THE PLAN (its acceptance criteria + description, appended below) IN PARALLEL with dev — the shift-left `gen_test_cases` step — so by the time you run, the cases usually already exist (as test-case rows with runnable scripts, executed by `run_test_cases`, or as committed tests on the branch). Your job is to RUN and JUDGE that suite, NOT to write it again. (a) RUN the existing test cases / suite for this issue and record pass/fail per case — a plan-asserting case that is RED on the branch is a NEW failure → `qa:fail` unless re-running that same case on the merge-base shows it was already red. (b) VERIFY each case actually asserts the PLAN, not just echoes the implementation: a case that would STILL PASS even if the code were wrong proves nothing; if it also passes on the merge-base it does not lock the new behavior — treat that criterion as UNCOVERED. This DISCRIMINATION check is the one place a baseline run of a PASSING thing earns its keep, so do it even when step 2 was skipped — but run only the individual plan-driven CASES against the base, never the whole build+lint+test matrix again. (c) BACKFILL only what's missing: a criterion with no covering case, or an obvious untested edge/error path (boundary, empty input, failure branch, auth check). Do NOT duplicate a case that already covers its criterion — redundant authoring is the cold-start cost this shift-left split exists to avoid. If NO cases exist at all (e.g. no QA squad configured, so the parallel gen never ran), author the full plan-driven set yourself as a fallback. Author against the PLAN in the project's framework (unit + Playwright/e2e against the running preview), mock external APIs, follow the repo's existing layout. (d) A criterion with NO valid covering case after your pass is a coverage GAP — list it. (e) MATERIALIZE THE SUITE INTO THE REPO when the gate ends `pass`: the suite must ACCRETE as committed, CI-runnable coverage — not live only in Agora's case rows. Write every automated case you ran (the pre-authored scripts plus your backfills) into the repo's OWN test layout — prefer the `agora-qa` MCP `write_test_file` tool; follow the repo's existing test dir and naming (`tests/`, `e2e/`, `__tests__/`, `*_test.go`…), falling back to `tests/agora/<issue-identifier>/` only when the repo has none. Adapt each script to run WITHOUT Agora-specific plumbing (base_url from an env var with a sane default, no daemon-port dependence) so the team's plain test command runs it forever. Then COMMIT the test files on the CHANGE'S branch (`test(qa): <issue identifier> — QA cases`) and push. From then on, re-verifying this behavior is `pnpm test` — a command, not an agent run. Do NOT materialize on `fail` (the fix loop re-runs the case rows), and NEVER modify non-test files in this step. CRITICAL: never weaken, skip, delete, or rewrite a case to go green — surfacing a real failure is the gate working, not a problem to hide. (5) VERDICT: post a comment with two sections — NEW (regressions this change introduced) and PRE-EXISTING (already red on baseline, out of scope) — listing every command with its baseline and branch exit code, which acceptance criterion each test case covers (the pre-authored cases plus any you backfilled, and any criterion left uncovered), and the screenshots. The verdict is `pass` when this change introduces NO new failure AND the plan-driven cases pass — so the implementation meets every criterion AND the smoke is clean — even if the repo carries pre-existing red. The verdict is `fail` when the change introduces or worsens a failure OR an implemented criterion's test fails. Never fabricate a green result, but never blame the change for pre-existing breakage. The platform captures your verdict into the `qa:pass`/`qa:fail` label automatically the moment your comment posts — do NOT also set the label yourself via CLI; that step is redundant and wastes tool calls. At the END of that comment, append a fenced ```qa-result code block containing ONLY a JSON object the editor's QA panel parses to render the result structured: `{"verdict":"pass"|"fail","summary":"<one line>","commit_sha":"<git rev-parse HEAD of the branch checkout you tested — run it, report it verbatim>","started_at":"<RFC3339 timestamp when you began the gate>","finished_at":"<RFC3339 timestamp when you finished>","commands":[{"cmd":"<command>","baseline_exit":<int|null>,"branch_exit":<int>,"kind":"pass"|"new_failure"|"pre_existing","error":"<short reason, ONLY for new_failure>"}],"screenshots":["<path-or-url>"],"phases":[{"phase":"checks"|"baseline"|"smoke"|"cases"|"materialize","started_at":"<RFC3339>","finished_at":"<RFC3339>","skipped":<bool>,"note":"<why, ONLY when skipped>"}]}` — `phases` is REQUIRED and is how the platform measures where a gate spends its time. Emit ONE entry per numbered step above, in order, using exactly those five phase names. For a step you RAN, give `started_at` + `finished_at` and omit `skipped`. For a step you SKIPPED, set `skipped":true` with a one-line `note` and OMIT the timestamps — do not invent a zero-length window. The BASELINE entry is mandatory EVEN WHEN YOU SKIP IT: `{"phase":"baseline","skipped":true,"note":"every branch command passed"}` is the single most useful datapoint the gate reports, because it is what proves the lazy baseline is paying off. Timestamps are wall-clock at your end and are read as durations only, so clock skew against the server does not matter. — `commit_sha` is REQUIRED whenever you had a git checkout: run `git rev-parse HEAD` on the CHANGE'S branch (not the baseline) and report exactly that; omit it only when there was no repository to test (a pure remote-URL smoke). — `baseline_exit` is null whenever no baseline run happened for that command — because it passed on the branch (the normal case: you only baseline what went red), or because it only exists on the branch (e.g. your new tests). A null baseline is never an error. `kind` is `new_failure` only when you ACTUALLY ran the command on the base, it passed there, and the branch failed. For EVERY `new_failure` command, set `error` to the ONE line that actually explains it — the failing assertion message or the last non-empty stderr line (e.g. `expected 200, got 500` or `AssertionError: title not trimmed`), NOT the full stack trace and NOT a restatement of the exit code. Omit `error` (or leave it empty) for `pass` and `pre_existing` commands. The JSON must be valid and self-contained (the human-readable sections above stay as well). Do NOT merge anything — your verdict is advisory and the human decides next.
