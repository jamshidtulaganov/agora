Open the pull/merge request for this issue's change — the code review PASSED with no blocking findings, and opening the request is the remaining step. (1) FIND THE BRANCH: the dispatch context names it. Confirm it exists on the remote (`git fetch origin <branch>`); if it does not, push the existing local branch as-is. Do NOT create a new branch, do NOT rebase, squash, amend, or otherwise rewrite the reviewed commits — the diff that passed review is the diff that must be proposed. (2) DO NOT WRITE CODE. This is not an implementation task: no source edits, no version bumps, no "small fix while I'm here". If you believe the change is incomplete, STOP and say so in a comment instead of editing — a change that moves after the review is a change nobody reviewed. (3) OPEN THE REQUEST against the base branch named in the dispatch context. On GitLab there is no `gh`: push the branch with the merge-request push options — `git push -o merge_request.create -o merge_request.target=<base> -o merge_request.remove_source_branch origin <branch>` — and if the branch is already pushed and the MR still does not exist, re-push with the same options (`git push -o merge_request.create ... --force-with-lease` ONLY if the remote branch is identical to yours; never force-push a different tree). On GitHub use `gh pr create --base <base> --head <branch> --fill`. (4) TITLE + BODY: title = the issue's title; body = a short summary of what changed and why, the issue key, and a line stating that the automated code review passed (name the reviewer agent and the reviewed commit sha from the dispatch context). Keep it factual — do not restate the whole diff. (5) NEVER MERGE and never approve it yourself. The merge decision belongs to a human; your job ends when the request is open. (6) REPORT: post a comment containing the merge/pull request URL **as a plain link on its own line** — the platform parses that URL to link the request to this issue, so a missing or mangled URL means the issue shows no request at all. Then append exactly ONE fenced ```open-pr-result code block containing ONLY a JSON object: `{"opened":true|false,"url":"<the MR/PR url, or empty>","branch":"<branch>","base":"<base branch>","reason":"<why not, when opened is false>"}`. If the request already existed, that is a SUCCESS: report its URL with `"opened":false` and `"reason":"already open"` rather than creating a duplicate.
