Commit this issue's PASSING E2E specs into the repository, on the SAME branch as the change under test — the specs stop being platform-only rows and become part of the project's committed regression suite that every future pipeline runs. (1) BRANCH: check out the EXISTING branch named in the dispatch context (the issue's open pull/merge request branch) — do NOT create a new branch, do NOT open a second pull/merge request, do NOT push to the default branch. If the named branch cannot be checked out, STOP and post why; do not invent a branch. (2) WHAT TO COMMIT: exactly the specs listed in the dispatch context below and nothing else. Each carries a case id and a compiled script that ALREADY PASSED against the reviewed change — commit that script as-is except for the mechanical edits in step (3). Never commit a spec that is not on the list (an unlisted case either never ran or last ran red, and committing it plants a red test in the repo). (3) LAYOUT + EDITS: put each spec where the repository ALREADY keeps its browser tests — look for an existing Playwright config (`playwright.config.*`) and its `testDir`, else an existing `tests/e2e/` or `e2e/` folder, and match the surrounding files' naming (`<feature>.spec.ts` / `.spec.mjs`); only if the repo has no browser tests at all, create `tests/e2e/` and add a minimal `playwright.config.ts` next to it. Name each file after the behavior it asserts, never after the case id. Mechanical edits ONLY: convert the standalone script into the repo's existing test idiom (e.g. wrap the body in `test(...)` and use the repo's fixtures/helpers when its other specs do), and replace any hardcoded QA-target host with the repo's existing base-URL mechanism (`baseURL` in the config / `process.env`). Do NOT rewrite the assertions, weaken them, or delete a check to make a spec pass — the assertions are the value being committed. Add a one-line comment at the top of each file naming the Agora case id and the issue key, so the committed spec is traceable back to the case it came from. (4) VERIFY BEFORE COMMITTING: run the specs you are about to commit through the repo's own runner (`npx playwright test <paths>` or the project's test script). Commit ONLY the ones that pass there. If a spec passes standalone but fails inside the repo harness, leave it OUT and say so in your comment — a red spec in the suite blocks every future pipeline, which is strictly worse than a missing one. (5) COMMIT + PUSH: one commit, message `test(e2e): add regression specs for <ISSUE-KEY>` with `[skip ci]` at the END of the subject line. The `[skip ci]` is REQUIRED and load-bearing: without it this push retriggers the branch pipeline, which re-runs the review/E2E chain, which commits again — an infinite loop. Push to the SAME branch. Touch NO product code, no dependency manifests beyond adding a test dependency the repo genuinely lacks, and no CI config. (6) REPORT: post a comment listing, per spec, the case id, the committed file path, and its verify result; name every listed case you did NOT commit and why. Then append exactly ONE fenced ```committed-specs code block containing ONLY a JSON array: `[{"case_id":"<uuid>","path":"<repo-relative path>","committed":true|false,"reason":"<why not, when committed is false>"}]`. Do NOT merge the pull/merge request — the merge decision belongs to a human.
