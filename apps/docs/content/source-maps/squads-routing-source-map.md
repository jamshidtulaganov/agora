# squads-routing — source map

This sidecar traces every load-bearing claim in `squads-routing.mdx` back to the exact symbol in the repo. Keep it in lockstep with the page.

| File | Symbols this page depends on |
|---|---|
| `server/internal/handler/squad.go` | `CreateSquad` — auto-adds leader as member with `Role: "leader"`; `UpdateSquad` — auto-adds new `leader_id` as member if not already one; `RemoveSquadMember` — `"cannot remove the squad leader; change leader first"`; `shouldEnqueueSquadLeaderOnAssign` — backlog parking + `isSquadLeaderReady`; `isSquadLeaderReady` — `service.AgentReadiness`, fail-closed on error; `enqueueSquadLeaderTask` — resolve squad, `canEnqueueSquadLeader`, `HasPendingTaskForIssueAndAgent`, `EnqueueTaskForSquadLeader`; `lastTaskWasLeader` → `GetLatestTaskIsLeaderForIssueAndAgent`; `RecordSquadLeaderEvaluation` — outcome enum, leader-only `403`, `X-Task-ID`, writes `squad_leader_evaluated` activity; `commentMentionsAnyone` — routing mention types |
| `server/internal/handler/squad_briefing.go` | `buildSquadLeaderBriefing` — 3 sections, Instructions appended only when non-empty; `squadOperatingProtocol` — coordinate-not-execute, mandatory evaluation, stop-after-dispatch, "no mention link = no delegation"; `buildSquadRoster` — leader self-row + members; `renderMemberRow` — skips archived/unresolvable (returns `""`); `formatRosterRow`; `formatMention` — `[@name](mention://<type>/<id>)`, must round-trip `util.ParseMentions`; human members use `member` + `user_id` |
| `server/internal/handler/daemon.go` | `ClaimTaskByRuntime` — squad-leader briefing injection guarded by squad-assigned + `squad.LeaderID == resp.Agent.ID`; appended (`+ "\n\n" + briefing`), not replaced; same injection on quick-create claim path |
| `server/internal/service/squad_no_action.go` | `HasSquadLeaderNoActionEvaluationForTask` — wraps the per-task `no_action` query; guards nil/invalid task |
| `server/internal/service/task.go` | `EnqueueTaskForSquadLeader` — `enqueueMentionTask(..., isLeader=true, ...)`; `enqueueMentionTask` — sets `IsLeaderTask: pgtype.Bool{Bool: isLeader, Valid: isLeader}`, rejects archived agent (`agent is archived`) and runtime-less agent; `CompleteTask` — `suppressNoActionComment` from `HasSquadLeaderNoActionEvaluationForTask`, skips synthesized fallback comment; `RerunIssue` / `enqueueRerunTask` — `isLeader = sourceTask.IsLeaderTask`, squad-assignee fallback `isLeader = true`; `maybeReTriggerSquadLeaderOnMemberFailure` — re-engages leader on member failure, guards on `task.IsLeaderTask`, archived/runtime, pending |
| `server/internal/service/autopilot.go` | `resolveAutopilotLeader` — squad branch returns `squad.LeaderID`; `errSquadArchived` on `squad.ArchivedAt.Valid`; `formatAdmissionReason` — `"squad leader "` prefix; direct `EnqueueTaskForSquadLeader` call for `create_issue`/`run_only` |
| `server/internal/handler/issue_child_done.go` | `notifyParentOfChildDone`; `triggerChildDoneSquad` — leader-role task, same-squad + shared-leader loop guards, archived/runtime/pending checks; `effectiveChildAgentOwner` |
| `server/internal/handler/comment.go` | `computeCommentAgentTriggers` (preview/create shared trigger set); `409 "squad leader recorded no_action; comments are not allowed for this task"` via `HasSquadLeaderNoActionEvaluationForTask`; sits beside the resumed-session parent-id `409` guard |
| `server/pkg/db/queries/agent.sql` | `CreateRetryTask` — selects `p.is_leader_task`; `CreateFailoverTask` — selects `p.is_leader_task`; `GetLatestTaskIsLeaderForIssueAndAgent`; `HasPendingTaskForIssueAndAgent` |
| `server/pkg/db/queries/activity.sql` | `HasSquadLeaderNoActionEvaluationForTask` — `EXISTS` on `activity_log` keyed by `issue_id` + `actor_type='agent'` + `actor_id` + `action='squad_leader_evaluated'` + `details->>'outcome'='no_action'` + `details->>'task_id'` |
| `server/internal/service/builtin_skills/agora-squads/SKILL.md` | agent-facing squad contract — "A squad is not an agent", leader routing, no member fan-out, `instructions` = leader briefing, archived leader fails closed at dispatch |

## Drift rule

When any documented symbol, flag, field, Result/outcome enum value, CLI form, or file path above moves or changes shape, update **`squads-routing.mdx` and this source-map sidecar in the same PR**. The built-in `agora-squads` skill is a source-traced contract shipped to agents; stale docs silently teach wrong squad routing/briefing/no_action behavior. This is the builtin_skills same-PR contract from the repo-root CLAUDE.md.
