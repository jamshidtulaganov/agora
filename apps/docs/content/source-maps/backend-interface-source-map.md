## backend-interface — source map

This page is a source-traced contract. Every documented identifier below is copied verbatim from the listed file. If a symbol moves or its meaning changes, update `backend-interface.mdx` **and** this sidecar in the **same PR** (the builtin_skills drift rule). A code change that lands without the matching doc change silently teaches stale behavior.

| File | Symbols this page depends on |
| --- | --- |
| `server/pkg/agent/agent.go` | `Backend`; `Backend.Execute(ctx, prompt, opts) (*Session, error)`; `ExecOptions` fields `Cwd`, `Model`, `SystemPrompt`, `MaxTurns`, `Timeout`, `ResumeSessionID`, `ExtraArgs`, `CustomArgs`, `McpConfig`, `ThinkingLevel`; `runContext(ctx, timeout)` (positive→`WithTimeout`, non-positive→`WithCancel`); `Session{Messages <-chan Message, Result <-chan Result}`; `MessageType` + constants `MessageText`="text", `MessageThinking`="thinking", `MessageToolUse`="tool-use", `MessageToolResult`="tool-result", `MessageStatus`="status", `MessageError`="error", `MessageLog`="log"; `Message` fields `Type`,`Content`,`Tool`,`CallID`,`Input`,`Output`,`Status`,`Level`,`SessionID`; `TokenUsage{InputTokens,OutputTokens,CacheReadTokens,CacheWriteTokens}`; `Result{Status,Output,Error,DurationMs,SessionID,Usage map[string]TokenUsage}`; `Result.Status` string-literal enum `"completed"`/`"failed"`/`"aborted"`/`"timeout"`/`"cancelled"` (no Go constants) |
| `server/pkg/agent/claude.go` | `claudeBackend.Execute`; `make(chan Message, 256)` (msgCh) + `make(chan Result, 1)` (resCh); deferred ordering `defer cancel()` / `defer close(msgCh)` / `defer close(resCh)` (LIFO → resCh closed last, after the single `resCh <- Result{...}` send); `trySend(ch, msg)` (select/default drop on full); `buildEnv` → `mergeEnv` → `isFilteredChildEnvKey` (strips `CLAUDECODE`, `CLAUDE_CODE_ENTRYPOINT`, `CLAUDE_CODE_EXECPATH`, `CLAUDE_CODE_SESSION_ID`, `CLAUDE_CODE_SSE_PORT`, `CLAUDECODE_*`); `exec.LookPath(execPath)`; `hideAgentWindow(cmd)`; `cmd.WaitDelay = 10 * time.Second`; `newStderrTail(newLogWriter(...), agentStderrTailBytes)` as `cmd.Stderr`; status assignment in the post-`cmd.Wait()` switch (`runCtx.Err()==DeadlineExceeded`→"timeout", `==Canceled`→"aborted", write-fail→"failed", non-zero exit→"failed"); `withAgentStderr(finalError, "claude", stderrBuf.Tail())`; `claudeResultUsage` (per-model `Usage`, prefers `modelUsage`) |
| `server/pkg/agent/stderr_tail.go` | `const agentStderrTailBytes = 2048`; `stderrTail` (mutex-guarded bounded buffer, trims to `max` from the end); `newStderrTail`; `stderrTail.Write`; `stderrTail.Tail`; `withAgentStderr(msg, label, tail)` → `msg + "; " + label + " stderr: " + tail`, returns `msg` when tail empty |
| `server/internal/daemon/daemon.go` | `Daemon.executeAndDrain(ctx, backend, prompt, opts, taskLog, taskID)`; `agentCtx, agentCancel := context.WithCancel(ctx)`; early-return on `backend.Execute` error with `nil` Session; `drainCtx` sizing (`opts.Timeout > 0` → `Timeout + 30*time.Second`; else `WithCancel`, no deadline — MUL-3064); drain `for`/`select` over `session.Messages` with `if !ok { goto drainDone }`; per-type batching (`text`/`thinking`/`tool_use`/`tool_result`/`error`) flushed every 500ms via `d.client.ReportTaskMessages`; `toolCount`, `inFlightTools`, `lastActivityAt`, `idleWatchdogFired`, `idleWatchdogThreshold`; outer `select` on `<-session.Result` / `<-drainCtx.Done()`; daemon re-tag to `"idle_watchdog"`; `drainCtx.Done()` synthesis of `"cancelled"` (context.Canceled) and `"timeout"` (DeadlineExceeded); `runIdleWatchdog(window=AgentIdleWatchdog, toolWindow=AgentToolWatchdog)` — tool-in-flight uses larger budget, `toolWindow<=0` disables, requires `len(messages)==0` before firing, calls `cancel()`; `idleWatchdogReason` |

### Drift rule

When a documented symbol, struct field, `Result.Status` literal, `MessageType` value, or CLI flag listed above moves, is renamed, or changes meaning, update `backend-interface.mdx` and this file in the same PR. CI/skill contracts treat a stale source map as a documentation regression.
