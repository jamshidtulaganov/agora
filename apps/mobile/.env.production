# Mobile production env — committed so external users can build a personal
# iPhone copy of Agora against the same backend as agora.dev on web.
# Loaded by the `*:prod` scripts via dotenv-cli (see package.json).
EXPO_PUBLIC_API_URL=https://api.agora.dev
EXPO_PUBLIC_WEB_URL=https://agora.dev
