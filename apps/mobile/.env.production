# Mobile production env — committed so external users can build a personal
# iPhone copy of Tandem against the same backend as tandem.dev on web.
# Loaded by the `*:prod` scripts via dotenv-cli (see package.json).
EXPO_PUBLIC_API_URL=https://api.tandem.dev
EXPO_PUBLIC_WEB_URL=https://tandem.dev
