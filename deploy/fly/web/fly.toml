# sd-agora-web — Next.js (standalone). Public entry point.
# Proxies /api, /ws, /auth, /uploads to the backend over the private network.
# REMOTE_API_URL is read at runtime by next.config rewrites(), so it is a plain
# env here (no rebuild needed to repoint the backend).
#
# Build context MUST be the repo root. Deploy from repo root:
#
#   fly deploy --config deploy/fly/web/fly.toml --dockerfile Dockerfile.web \
#     --build-arg REMOTE_API_URL=http://sd-agora-backend.internal:8080 .

app = "sd-agora-web"
primary_region = "fra"

[env]
  NODE_ENV = "production"
  PORT = "3000"
  HOSTNAME = "0.0.0.0"
  # Private 6PN address of the backend — never leaves the org network.
  REMOTE_API_URL = "http://sd-agora-backend.internal:8080"
  # Docs site: /docs/:path* is reverse-proxied here (next.config rewrites,
  # read at runtime like REMOTE_API_URL).
  DOCS_URL = "https://sd-agora-docs.fly.dev"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-1x"
  memory = "1gb"

[[restart]]
  policy = "always"
